
@{
    ViewData["Title"] = "Home";
}
@model BlogAdmin.Controllers.MainPageData

<style>

</style>

@*<link rel="stylesheet" href="../css/bootstrap.css">*@
<script src="~/js/bootstrap.js"></script>

<link rel="stylesheet" href="/lib/bootstrap/dist/css/bootstrap.css/bootstrap.min.css" />
<link rel="stylesheet" href="bootstrap.css" />
<script src="~/js/bootstrap.js"></script>

<h1>Blog</h1>
<hr>
<div class="container">
    <p>&nbsp;</p>
    <div class="row ">
        <div class="col-xs-4 headercol  ">
            <h2>Categories</h2>
        </div>
        <div class="col-xs-4 ">
            <input type="button" value="Add" class="btn btn-primary" onclick="location.href='@Url.Action("Index", "Category")'"></>
        </div>
    </div>

    @foreach (var item in Model.ListOfCategories)
    {
        <div class="row">
            <div class="col-xs-10">
                <a href="@Url.Action("Edit", "Category" , item)">@item.title</a>
             </div>
        </div>       
    }

        <p>&nbsp;</p>
        <p>&nbsp;</p>
        <div class="row">
            <div class="col-xs-4 headercol ">
                <h2>Posts</h2>
            </div>
            <div class="col-xs-4">
                <input type="button" value="Add" class="btn btn-primary" onclick="location.href='@Url.Action("Index", "Post")'"></>
            </div>
        </div>

            @foreach (var item in Model.ListOfPosts)
            {
                <div class="row">
                    <div class="col-xs-10">
                        <a href="@Url.Action("Edit", "Post" , item)">@item.title</a>
                    </div>
                </div>
            }
    
    </div>


